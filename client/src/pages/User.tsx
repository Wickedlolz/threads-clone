import UserHeader from '../components/UserHeader';
import UserThread from '../components/UserThread';

const User = () => {
    return (
        <div>
            <UserHeader />
            <UserThread />
            <UserThread />
            <UserThread />
            <UserThread />
        </div>
    );
};

export default User;
